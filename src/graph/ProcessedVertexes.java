package graph;

public interface ProcessedVertexes {
	public void ajouter(Vertex element); // Ajouter l'élément element à la liste

	public boolean contient(Vertex element); //Renvoie True si l'element est dans l'ArrayList
}
